---
import BaseLayout from '../../layouts/BaseLayout.astro';
import IonsPage from '../../features/ions/IonsPage';
import type { SupportedLocale } from '../../types/i18n';
const locale = (Astro.currentLocale ?? 'pl') as SupportedLocale;
---

<BaseLayout title="Jony" description="Informator jonów: kationy i aniony, ładunki, kwasy macierzyste.">
  <IonsPage locale={locale} client:idle />
</BaseLayout>
