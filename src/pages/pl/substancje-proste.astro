---
import BaseLayout from '../../layouts/BaseLayout.astro';
import SimpleSubstancesPage from '../../features/simple-substances/SimpleSubstancesPage';
import type { SupportedLocale } from '../../types/i18n';

const locale = (Astro.currentLocale ?? 'pl') as SupportedLocale;
---

<BaseLayout title="Substancje proste" description="Katalog substancji prostych: metale, niemetale, gazy szlachetne. Wzory, masy molowe, stany skupienia.">
  <SimpleSubstancesPage locale={locale} client:idle />
</BaseLayout>
