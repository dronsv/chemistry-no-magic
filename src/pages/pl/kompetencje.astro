---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CompetencyGraphIsland from '../../features/competency/CompetencyGraphIsland';
import type { SupportedLocale } from '../../types/i18n';
const locale = (Astro.currentLocale ?? 'pl') as SupportedLocale;
---

<BaseLayout title="Mapa kompetencji" description="Graf zależności umiejętności: od prostych do zaawansowanych">
  <CompetencyGraphIsland locale={locale} client:idle />
</BaseLayout>
