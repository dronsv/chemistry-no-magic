---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import SubstancesPage from '../../../features/substances/SubstancesPage';
import type { SupportedLocale } from '../../../types/i18n';
const locale = (Astro.currentLocale ?? 'pl') as SupportedLocale;
---

<BaseLayout title="Substancje" description="Klasyfikacja substancji: tlenki, kwasy, zasady, sole. Właściwości, nazewnictwo i ćwiczenia.">
  <SubstancesPage locale={locale} client:idle />
</BaseLayout>
