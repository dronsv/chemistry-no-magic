---
import BaseLayout from '../../layouts/BaseLayout.astro';
import IonsPage from '../../features/ions/IonsPage';
import type { SupportedLocale } from '../../types/i18n';
const locale = (Astro.currentLocale ?? 'en') as SupportedLocale;
---

<BaseLayout title="Ions" description="Ion reference: cations and anions, charges, parent acids.">
  <IonsPage locale={locale} client:idle />
</BaseLayout>
