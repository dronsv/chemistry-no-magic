---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import PhysicalFoundationsPage from '../../../features/physical-foundations/PhysicalFoundationsPage';
import type { SupportedLocale } from '../../../types/i18n';
const locale = (Astro.currentLocale ?? 'en') as SupportedLocale;
---

<BaseLayout
  title="Physical Foundations of Chemistry"
  description="Explanations of key physical concepts, mechanisms, and bridges between physics and chemistry: temperature, thermal motion, energy levels, exchange stabilization."
  lang="en"
>
  <PhysicalFoundationsPage locale={locale} client:idle />
</BaseLayout>
