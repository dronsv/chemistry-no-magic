# 🧰 Полный технологический стек
## Проект: «Химия без магии»
Версия: 2026-02-15

Цель: зафиксировать рекомендуемый стек для CDN-first реализации курса без backend, с интерактивом и локальной адаптивностью.

---

# 1. Frontend

- Astro — static-first сайт
- React islands — интерактивные компоненты (тренажёр, графики, конфигурации)
- TypeScript — типизация данных и контрактов
- Markdown/MDX — учебный контент

---

# 2. Данные и валидация

- JSON bundles (immutable)
- JSON Schema + валидатор (CI)
- Mermaid для схем (документация и часть контента)

---

# 3. Build pipeline (data → bundles)

- генератор `manifest.json` и `/data/{hash}/`
- генерация индексов (`indices/*`)
- линтер/валидатор ссылок (id/ref)
- CI (GitHub Actions/GitLab CI) для проверки и сборки

---

# 4. Хостинг и CDN

- любые платформы с CDN: Cloudflare Pages / Vercel / Netlify / S3+CDN
- политика кеширования:
  - `/data/{hash}/*` → immutable (год)
  - `/data/latest/manifest.json` → короткий кеш

---

# 5. Локальное хранение и офлайн

- IndexedDB — попытки (attempts), кэш
- localStorage — BKT `P(L)` и настройки
- PWA (рекомендуется):
  - offline режим для mvp_pack
  - prefetch основных бандлов

---

# 6. Поиск

- client-side search (на основе `indices/search_index.json`)
- фильтрация по классам/ионам/компетенциям

---

# 7. Визуализации

- SVG/Canvas — орбитали, энергетические профили
- Mermaid — схемы и генетические цепочки
- (опционально) d3 — если потребуется более сложная интерактивность

---

# 8. Тестирование

- unit tests:
  - классификация/номенклатура
  - guards автогенерации реакций
  - обновление BKT
- контент-тесты:
  - целостность ссылок
  - консистентность индексов

---

# 9. Библиотеки (ориентир)

- Astro, React, TypeScript
- Ajv (JSON Schema)
- idb (IndexedDB wrapper)
- MiniSearch/Lunr (поиск)
- Mermaid renderer (если рендерить на сайте)

---

# 10. Расширение после MVP

- синхронизация прогресса через optional backend (не обязательно)
- аналитика — только с явным согласием, по возможности локально/анонимно
