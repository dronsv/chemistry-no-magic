# 🧠 Алгоритм адаптивного маршрута обучения
## Проект: «Химия без магии»
Версия: 2026-02-15

Документ определяет формальный алгоритм, который:
- быстро оценивает пробелы,
- строит персональный маршрут,
- минимизирует время подготовки,
- работает полностью локально (в браузере).

Связанные документы:
- `01_learning_plan.md` (логика курса)
- `03_competency_map.md` (список компетенций)
- `04_competency_graph_mermaid.md` (зависимости)
- `05_task_competency_matrix.md` (связь заданий с компетенциями)

---

# 1. Объекты данных

## 1.1 Компетенция
- `id`: string
- `score`: float ∈ [0,1]
- `confidence`: float ∈ [0,1] (насколько оценка надёжна)
- `attempts`: int (сколько раз мерили)
- `last_seen`: timestamp

## 1.2 Результат задания (attempt)
- `exercise_id`
- `timestamp`
- `seed`
- `is_correct` (bool)
- `time_spent_sec`
- `used_hint` (bool)
- `mistake_type` (optional: enum)
- `competency_credits`: список (competency_id, weight, role=P/S/O)

Хранение:
- attempts → IndexedDB
- competency_state → localStorage (агрегаты)


---

# 0. Связь с формальной матмоделью (BKT)
Этот документ описывает **policy layer** (как выбирать задания и строить маршрут).
Формальные вероятностные обновления компетенций заданы в `07_adaptive_bkt_math_model.md`.
---

# 2. Диагностический протокол (15–20 минут)

## 2.1 Цель
Быстро оценить **все** компетенции с минимальным числом заданий.

## 2.2 Правило подбора
Используем матрицу `05_task_competency_matrix.md`:
- выбираем набор заданий так, чтобы каждая компетенция имела ≥1 измерение **P**
- для узлов-«бутылочных горлышек» (PT, EC, OS, CL, RXE) — ≥2 измерения (разными типами)

## 2.3 Адаптивная диагностика (ветвление)
Если ученик проваливает критический узел:
- прекращаем углубляться в производные компетенции,
- добираем prerequisites и повторно измеряем.

---

# 3. Обновление оценок компетенций

## 3.1 Базовые правила начисления
Пусть `Δ` зависит от результата:

- Верно быстро (time < T_fast, без подсказок) → +0.05
- Верно медленно или с подсказкой → +0.02
- Ошибка → −0.03

Границы T_fast задаются типом задания (например, 30–60 секунд).

## 3.2 Взвешивание по роли
Если компетенция была:
- Primary (**P**) → weight = 1.0
- Secondary (**S**) → weight = 0.5
- Optional (**O**) → weight = 0.2

Тогда итоговое изменение:
`score += Δ * weight`

## 3.3 Обновление confidence
- confidence растёт с числом попыток и стабильностью ответов
- минимальная модель:
  - `confidence = min(1, attempts / 10)`
  - корректируется вниз при высокой вариативности (опционально)

## 3.4 Ограничения
- score всегда клэмпится в [0,1]
- быстрый рост ограничиваем: не более +0.10 за 10 минут на одну компетенцию (анти-«рандом»)

---

# 4. Пороговые режимы

- **< 0.6**: пробел (ремедиация обязательна)
- **0.6–0.8**: рабочий уровень (укрепление)
- **> 0.8**: компетенция закрыта (переход вперёд, редкие контрольные)

---

# 5. Построение маршрута (route planning)

## 5.1 Вход
- текущие `score` по компетенциям
- граф зависимостей (prerequisites)
- цель (ОГЭ): целевой порог, напр. 0.8 на ключевых компетенциях

## 5.2 Шаг 1: определить «фронтир пробелов»
Находим компетенции, где:
- score < 0.6
- и они находятся ближе всего к корню графа (т.е. блокируют многие темы)

Приоритет = (число зависимых компетенций) × (0.6 - score)

## 5.3 Шаг 2: выбрать микро-модуль восстановления
Для выбранной компетенции X:
- берем 1–2 микролекции (очень коротко, 3–7 минут)
- даём 3–7 заданий, где X является **P**
- добавляем 1–2 задания на prerequisites X (если они < 0.7)

## 5.4 Шаг 3: контроль
После микроблока:
- контрольное задание (новый seed)
- если score X ≥ 0.6 → возвращаемся к плану
- иначе → ещё один цикл (не более 2 циклов подряд, потом смена подхода/типа задач)

---

# 6. Два режима обучения

## 6.1 Режим «С нуля»
Правило:
- следуем по линейной структуре модулей
- но всё равно используем диагностику для ускорения

Гейт:
- перед переходом к модулю проверяем, что prerequisites ≥ 0.6

## 6.2 Режим «Быстро» (есть знания)
Правило:
- стартуем с диагностики
- строим маршрут по «фронтиру пробелов»
- теорию показываем только точечно, как ремедиацию

---

# 7. Политика повторений (spaced repetition, упрощённо)

Чтобы не забывалось:
- каждая закрытая компетенция (>0.8) попадает в «повторение»
- повторение запускается:
  - на следующий день (или следующий запуск)
  - затем через 3 дня
  - затем через 7 дней
- если ошибка на повторении → компетенция падает в «0.6–0.8» и укрепляется

Реализуется локально, без сервера.

---

# 8. Прозрачность для пользователя (без магии)

UI обязан показывать:
- «почему вам сейчас это предлагают» (какая компетенция просела)
- «какой следующий шаг»
- «какие зависимости мешают продвижению» (из графа)

---

# 9. Критерии успеха алгоритма

- Диагностика ≤ 20 минут
- Время до достижения целевого профиля (≥0.8 на ключевых) минимизируется
- Количество повторяющихся ошибок снижается
- Пользователь видит объяснимые причины выбора задач
