# 📊 Формальная математическая модель адаптивности (BKT)
## Проект: «Химия без магии»
Версия: 2026-02-15

Связанные документы (в той же папке):
- `03_competency_map.md` — перечень компетенций
- `04_competency_graph_mermaid.md` — зависимости компетенций
- `05_task_competency_matrix.md` — связь типов заданий с компетенциями
- `06_adaptive_route_algorithm.md` — алгоритм маршрута (policy layer)

---

# 1. Цель модели

BKT (Bayesian Knowledge Tracing) даёт:
- вероятностную оценку владения каждой компетенцией,
- устойчивость к угадыванию,
- понятную логику обновления после попыток,
- основу для адаптивного выбора заданий.

Модель работает полностью локально (браузер), без сервера.

---

# 2. Сущности и параметры

## 2.1 Компетенция (skill)
Для каждой компетенции `c` хранится вероятность владения:

- `P(L_c)` — вероятность, что ученик владеет компетенцией `c` в данный момент.

Эта величина и есть “score” компетенции, но с вероятностной интерпретацией.

## 2.2 Параметры BKT (на компетенцию)
Для каждой компетенции `c` задаются параметры:

- `P(L0_c)` — начальная вероятность владения до наблюдений
- `P(T_c)` — вероятность “обучения/перехода” после одной попытки (learning rate)
- `P(S_c)` — вероятность ошибки при владении (slip)
- `P(G_c)` — вероятность угадывания при невладении (guess)

Параметры задаются как часть `bkt_params.json` и могут быть:
- одинаковыми для всех (MVP)
- различающимися по компетенциям и/или типам заданий (после MVP)

---

# 3. Наблюдение и его качество

Каждая попытка даёт наблюдение `O ∈ {correct, wrong}`.

Дополнительно учитывается качество ответа:
- время выполнения `t`
- использование подсказки `hint`

Это вводится через модификаторы (см. §6), не меняя базовую формулу BKT.

---

# 4. Формулы обновления (классический BKT)

Пусть перед попыткой вероятность владения `P(L)` известна.

## 4.1 Апостериор после наблюдения

### Если ответ верный (`correct`):
P(L | correct) = [ P(L)·(1−S) ] / [ P(L)·(1−S) + (1−P(L))·G ]

### Если ответ неверный (`wrong`):
P(L | wrong) = [ P(L)·S ] / [ P(L)·S + (1−P(L))·(1−G) ]

## 4.2 Переход обучения (learning transition)

После обновления по наблюдению применяем обучение:

P(L') = P(L | O) + (1 − P(L | O))·T

`P(L')` — значение, которое сохраняем как текущее состояние компетенции.

---

# 5. Инициализация и границы

## 5.1 Инициализация `P(L0)`
Варианты:
- по умолчанию (MVP): одинаково, например `0.25`
- по диагностике: по результатам стартового теста задаём разные `P(L0)`

## 5.2 Ограничения
- `P(L)` хранится в `(ε, 1−ε)` например ε=0.001
- параметры `S, G, T` ограничены разумными диапазонами

---

# 6. Модификаторы для времени и подсказок (практичный BKT для курса)

Чтобы “верно с подсказкой” не равнялось “верно сам”.

## 6.1 Модификация наблюдения через effective guess/slip

Вводим множители по попытке:

- если `hint = true`: увеличиваем `G_eff`, увеличиваем `S_eff`
- если `t` очень большое: немного увеличиваем `S_eff`
- если `t` очень маленькое: немного уменьшаем `S_eff`

Рекомендуемые правила (MVP):

- `hint=true`:
  - `G_eff = min(0.60, G + 0.25)`
  - `S_eff = min(0.60, S + 0.10)`
- `t > T_slow`:
  - `S_eff = min(0.50, S_eff + 0.05)`
- `t < T_fast`:
  - `S_eff = max(0.01, S_eff − 0.03)`

Далее используем формулы из §4 с `S_eff`, `G_eff`.

## 6.2 Модификация learning rate

Если ученик решил сам (без подсказки):
- `T_eff = min(0.35, T + 0.05)`

Если с подсказкой:
- `T_eff = max(0.01, T − 0.03)`

---

# 7. Связь компетенций (пререквизиты) поверх BKT

BKT считает компетенции независимыми. Зависимости задаются графом (`04_competency_graph_mermaid.md`) на уровне policy.

## 7.1 Влияние пререквизитов на выбор заданий
Если `A` зависит от `B`, и `P(L_B) < 0.6`, то:
- не назначаем задания, где `A` является primary,
- либо назначаем их только как диагностические.

## 7.2 Мягкое ограничение отображаемого прогресса (UI)
Можно показывать “эффективную готовность”:

P_eff(A) = min( P(L_A), P(L_B) + δ ), где δ≈0.10

---

# 8. Выбор следующего задания (objective)

Для набора кандидатов заданий выбираем то, что максимизирует ожидаемую полезность.

Пример скоринга задания `e`:

Score(e) = Σ_{c∈skills(e)} w_role(c,e) · w_bottleneck(c) · (target − P(L_c)) · w_uncertainty(c)

где:
- `w_role` берётся из матрицы (P/S/O),
- `w_bottleneck` — из графа (влияние узла),
- `w_uncertainty` растёт при малом числе попыток.

---

# 9. Параметры по умолчанию (MVP)

Рекомендуемые стартовые параметры (одинаковые для всех компетенций):
- `P(L0)=0.25`
- `T=0.12`
- `S=0.10`
- `G=0.20`

---

# 10. Пороговые уровни

- базовый: `P(L) ≥ 0.6`
- уверенный: `P(L) ≥ 0.8`
- автоматизм: `P(L) ≥ 0.93` (опционально)

---

# 11. Хранение состояния

- `bkt_state` (P(L) по компетенциям) → localStorage
- попытки (attempts) → IndexedDB

При смене версии бандлов:
- сохраняем state,
- добавляем новые компетенции с `P(L0)`,
- при необходимости — миграция параметров.
