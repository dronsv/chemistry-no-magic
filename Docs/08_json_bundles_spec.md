# 📦 Спецификация JSON-бандлов (CDN-first)
## Проект: «Химия без магии»
Версия: 2026-02-15

Цель: формально описать структуру статических данных в CDN, которые обеспечивают:
- справочник веществ и ионов,
- правила классификации и номенклатуры,
- генерацию реакций и заданий,
- диагностику и адаптивность (BKT),
- быстрый client-side поиск.

Связанные документы:
- `02_technical_spec.md`
- `07_adaptive_bkt_math_model.md`
- `09_mvp_roadmap.md`

---

# 1. Принцип версионирования и кеширования

## 1.1 Immutable bundles
Все файлы версии бандла лежат по пути:
`/data/{bundle_hash}/...`
и отдаются как immutable (год).

## 1.2 Точка входа
`/data/latest/manifest.json` — короткий кеш (1–5 минут).

Клиент:
1) грузит `manifest.json`
2) получает `bundle_hash`
3) грузит остальное только из `/data/{bundle_hash}/...`

---

# 2. Дерево данных

Рекомендуемая структура внутри `/data/{bundle_hash}/`:

- `manifest.json`
- `elements.json`
- `ions.json`
- `rules/`
  - `classification_rules.json`
  - `naming_rules.json`
  - `solubility_rules_light.json`
  - `activity_series.json`
  - `applicability_rules.json`
  - `bkt_params.json`
- `templates/`
  - `reaction_templates.json`
  - `task_templates.json`
- `substances/`
  - `{substance_id}.json`
- `indices/`
  - `substances_index.json`
  - `by_class/{class}.json`
  - `by_ion/{ion_id}.json`
  - `by_competency/{competency_id}.json`
  - `search_index.json` (опционально)

---

# 3. Manifest

`manifest.json` должен содержать:
- `bundle_hash`
- `schema_versions`
- `entrypoints` (пути к ключевым файлам)
- `packs` (опционально: mvp/full)
- `stats` (кол-во веществ/шаблонов/индексов)

---

# 4. Контракты сущностей (уровень ТЗ)

## 4.1 elements.json
- Z, symbol, name_ru
- group, period
- metal_type (metal/nonmetal/metalloid)
- typical_oxidation_states (школьные)
- electronegativity (опционально)
- hints для электронных конфигураций (опционально)

## 4.2 ions.json
- id, formula, charge, type (anion/cation)
- name_ru (анион) / name_ru_genitive (катион)
- parent_acid (если применимо)
- tags для логики газов/осадков (carbonate/sulfite/halide/...)

## 4.3 rules/*
- детерминированные правила
- списки исключений (что “не идёт” в школьных условиях)
- параметры BKT (по компетенциям)

## 4.4 templates/*
- `task_templates.json` содержит mapping «задание → компетенции (P/S/O)» и seed-параметры
- `reaction_templates.json` содержит условия (в т.ч. катализатор) без участия в балансе

## 4.5 substances/{id}.json
MVP:
- id, formula
- optional: fixed name_ru
- optional: notes/links to theory blocks
- optional: tags

Позже:
- industrial production
- occurrence (атмосфера/кора/океан)
- applications
- resonance refs

---

# 5. Индексы (обязательны для скорости)

Цель: избегать O(N²) перебора на клиенте.

Минимум:
- `by_class/*` — кислоты/основания/оксиды/соли
- `by_ion/*` — CO3, SO3, S2, halides и т.д.
- `by_competency/*` — банк упражнений на каждую компетенцию

---

# 6. Packs (mvp/full)

Поддержать режимы:
- `mvp_pack`: быстрый старт (минимум данных)
- `full_pack`: расширенный справочник

Клиент по умолчанию грузит `mvp_pack` и догружает `full_pack` по запросу.

---

# 7. Критерии качества данных

- JSON Schema в CI
- ссылочная целостность (id/ref)
- стабильные идентификаторы
- корректная политика кеша (immutable для hash-версий)
