/**
 * Replace with real MCP resource registration once the SDK version is pinned.
 */
export function registerResources(_server: unknown): void {
  // Example resource ideas:
  // ontology://schema/kinds
  // ontology://policy/admission
  // ontology://entity/{ref}
}
